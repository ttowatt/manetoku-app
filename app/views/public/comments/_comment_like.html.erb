<div id="comment-like-<%= comment.id %>" class="d-flex align-items-center mr-2">
  <% liked = comment.comment_likes.exists?(user: current_user) %>

  <% if liked %>
    <%= link_to post_comment_comment_likes_path(comment.post, comment, liked),
                  method: :delete,
                  remote: true,
                  class: 'comment-like-button btn btn-link p-0' do %>
      <i class="fa-solid fa-heart" style="color:red;"></i>
    <% end %>
  <% else %>
    <%= link_to post_comment_comment_likes_path(comment.post, comment),
                  method: :post,
                  remote: true,
                  class: 'comment-like-button btn btn-link p-0' do %>
      <i class="fa-regular fa-heart"></i>
    <% end %>
  <% end %>

  <span class="ml-1"><%= comment.comment_likes.count %></span>
</div>
