var likeDiv = document.querySelector("#comment-like-<%= @comment.id %>");
if (likeDiv) {
  likeDiv.innerHTML = '<%= j render partial: "public/comments/comment_like", locals: { comment: @comment } %>';
}

