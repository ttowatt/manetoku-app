<%= render "public/header2" %>
<div class="container" style="margin-top:166px;">
  <div class="row justify-content-center">
    <div class="col-12">
      <div class="card shadow-sm">
        <div class="card-body text-center">
          <h2 class="card-title mb-4 text-danger">本当に退会しますか？</h2>
          <p class="card-text mb-4">
            退会すると、これまでの投稿やデータがすべて削除されます。<br>
            この操作は取り消せません。
          </p>

          <div class="d-flex justify-content-center">
            <%= link_to "退会する",
                        withdraw_path,
                        method: :delete,
                        data: { confirm: "本当に退会しますか？" },
                        class: "btn btn-danger mx-2 px-4" %>

            <%= link_to "キャンセル",
                        user_path(current_user),
                        class: "btn btn-secondary mx-2 px-4" %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
