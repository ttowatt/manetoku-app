const postLikeElement = document.getElementById("post-like-<%= @post.id %>");
if (postLikeElement) {
  postLikeElement.innerHTML = `<%= j(render partial: 'public/posts/post_like', locals: { post: @post }) %>`;
}